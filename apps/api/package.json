{
  "name": "api",
  "version": "1.0.0",
  "scripts": {
    "dev": "go run cmd/server/main.go",
    "build": "go build -o bin/api cmd/server/main.go",
    "start": "npm run clean && npm run build && ./bin/api",
    "clean": "rm -rf bin"
  }
}
